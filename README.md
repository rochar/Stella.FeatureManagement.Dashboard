# Stella.FeatureManagement.Dashboard
Adds a ready-to-use web dashboard to ASP.NET Core applications for managing feature flags powered by Microsoft.FeatureManagement.  It provides a secured React-based SPA, embedded and served as a static resource, together with REST APIs to view, enable, disable, and configure feature flags at runtime
