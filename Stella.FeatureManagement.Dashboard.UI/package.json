{
  "name": "stella-feature-dashboard-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "dotnet gitversion /showvariable SemVer > .version.tmp && node -e \"const v=require('fs').readFileSync('.version.tmp','utf8').trim(); console.log('Building version:', v); require('fs').writeFileSync('version.json',JSON.stringify({version:v})); require('fs').unlinkSync('.version.tmp');\" && tsc && vite build",
    "build:dev": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
